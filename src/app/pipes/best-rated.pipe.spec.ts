import { BestRatedPipe } from './best-rated.pipe';

describe('BestRatedPipe', () => {
  it('create an instance', () => {
    const pipe = new BestRatedPipe();
    expect(pipe).toBeTruthy();
  });
});
